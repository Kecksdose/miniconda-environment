sudo: required

branches:
  only:
  - master

services:
  - docker

before_install:
  - printenv
  - docker pull centos:latest
  - docker run -d centos:latest centos_container /bin/bash -c "sleep 100000" --name=centos_container
  - docker exec -it centos_container /bin/bash

script:
 - printenv
