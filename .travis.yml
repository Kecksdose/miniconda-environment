sudo: required

branches:
  only:
  - master

services:
  - docker

before_install:
  - printenv
  - docker pull centos:latest
  - docker run -ti --name=centos_container centos:latest bash

script:
 - echo "Hello container"
