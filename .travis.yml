sudo: required

branches:
  only:
  - master

services:
  - docker

before_install:
  - printenv
  - docker pull centos:latest
  - docker run -d centos:latest --name centos_container /bin/bash -c "sleep 100000"
  - docker exec -it centos_container /bin/bash

script:
 - printenv
